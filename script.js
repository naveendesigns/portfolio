const cards=document.querySelectorAll('.card');cards.forEach(c=>c.addEventListener('click',()=>location.href=c.dataset.u));if(!matchMedia('(prefers-reduced-motion: reduce)').matches){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.style.opacity=1;e.target.style.transform='none';io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('section,.card,.step,.row').forEach(e=>{e.style.opacity=0;e.style.transform='translateY(28px)';e.style.transition='opacity .75s ease,transform .75s ease';io.observe(e)})}